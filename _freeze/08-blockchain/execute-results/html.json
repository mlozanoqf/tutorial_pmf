{
  "hash": "3a5c45fee8560d1c954de51872a9d349",
  "result": {
    "engine": "knitr",
    "markdown": "# Blockchain\n\n\n\n\n\n\n\n\n\nThe previous chapters worked with market data, returns, trading rules, asset\npricing, portfolio allocation, and market risk. This chapter changes the object\nof analysis. A blockchain is treated here as a financial-computing mechanism:\na way to maintain a shared record of transfers, validate changes to that\nrecord, and coordinate incentives across participants that do not all rely on a\nsingle central ledger.\n\nThe financial question is direct: how can a system record transfers of value\nwhen many participants can read the ledger, submit transactions, and verify the\nhistory? The answer combines several ideas:\n\n* a ledger that records transactions;\n* blocks that group transactions;\n* cryptographic hashes that link blocks;\n* digital signatures that authorize transactions;\n* a consensus rule that tells nodes which history to accept;\n* incentives that reward participants who help maintain the ledger.\n\nThe chapter has three layers. The first layer is a generic blockchain model:\nwe build a small ledger, define valid transactions, group transactions into\nblocks, link blocks with hashes, and validate the resulting history. The second\nlayer uses Bitcoin as the main proof-of-work case. Bitcoin is useful because it\nturns the generic mechanism into an economic system with miners, fees, scarce\nissuance, public verification, and costly history rewriting. The third layer\nintroduces tokenization as a financial extension, where a ledger records\nownership units and cash-flow allocation rules.\n\nThe route is therefore:\n\n$$\n\\mathrm{generic\\ ledger\\ mechanism}\n\\longrightarrow\n\\mathrm{Bitcoin\\ proof\\ of\\ work\\ case}\n\\longrightarrow\n\\mathrm{tokenized\\ financial\\ claims}.\n$$\n\nThe examples are intentionally small. They are meant to show the logic of the\nsystem, not to reproduce a production blockchain.\n\nA useful way to read the chapter is through four frictions: a user may try to\nspend the same value twice; block space can become scarce; settlement becomes\nmore reliable as confirmations accumulate; tokenized claims need a connection\nbetween the ledger record and the legal or operational asset. These frictions\nmake blockchain a financial topic, because they connect technology to scarcity,\nincentives, custody, and settlement.\n\n## Generic blockchain model\n\nThe generic model abstracts from the details of any specific blockchain. It\nuses an account-based ledger because balances are easy to read: each wallet has\na balance, transactions move value across wallets, fees compensate the block\nproducer, and the final ledger state can be checked directly. Bitcoin uses a\ndifferent accounting model, based on unspent transaction outputs, and that\ndifference is discussed later.\n\nThe generic model answers four questions:\n\n| Component | Question | Financial role |\n|---|---|---|\n| Ledger state | Who owns what before and after transfers? | Records balances or ownership claims |\n| Transaction rule | Which transfers are valid? | Prevents invalid spending and malformed transfers |\n| Block structure | Which transactions enter the next ledger update? | Groups accepted transfers into an auditable batch |\n| Consensus rule | Which history should nodes extend? | Coordinates participants around one accepted ledger |\n\nThe important sequence is:\n\n$$\n\\mathrm{current\\ ledger}\n\\rightarrow\n\\mathrm{valid\\ transactions}\n\\rightarrow\n\\mathrm{candidate\\ block}\n\\rightarrow\n\\mathrm{accepted\\ block}\n\\rightarrow\n\\mathrm{updated\\ ledger}.\n$$\n\n**A shared ledger problem**\n\nA conventional financial ledger records who owns what and how balances change\nafter transactions. In a centralized system, one institution can update the\nledger, reject invalid transfers, and resolve conflicts. A blockchain moves\npart of that logic into a shared protocol. Many participants can hold copies of\nthe ledger, check the same rules, and agree on the accepted history.\n\nThe most basic financial state is a table of balances. Suppose four wallets\nstart with the following balances.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbalances <- tibble(\n  wallet = c(\"Alice\", \"Bob\", \"Carol\", \"Dave\", \"Miner\"),\n  balance = c(100, 35, 15, 5, 0)\n)\n\nbalances\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 2\n  wallet balance\n  <chr>    <dbl>\n1 Alice      100\n2 Bob         35\n3 Carol       15\n4 Dave         5\n5 Miner        0\n```\n\n\n:::\n:::\n\n\n\n\nA transaction changes that state. If wallet $A$ sends amount $x$ to wallet\n$B$ and pays fee $f$, the accounting update is:\n\n$$\n\\mathrm{Balance}_A^{\\prime} = \\mathrm{Balance}_A - x - f,\n\\qquad\n\\mathrm{Balance}_B^{\\prime} = \\mathrm{Balance}_B + x,\n\\qquad\n\\mathrm{Balance}_{\\mathrm{miner}}^{\\prime}\n= \\mathrm{Balance}_{\\mathrm{miner}}+f.\n$$\n\nThe transfer is valid only if the sender has enough balance and the transaction\nformat is acceptable. The rule can be written as:\n\n$$\n\\mathrm{Balance}_A \\geq x+f,\n\\qquad\nx>0,\n\\qquad\nf \\geq 0.\n$$\n\nThe rest of the generic model turns this accounting idea into a blockchain\nworkflow. The ledger state tells us the balances at a point in time. The block\nhistory tells us how the ledger reached that state. This distinction matters in\nfinance because a balance without an auditable path is weaker than a balance\nthat can be traced through accepted transactions.\n\nAt this point there is no Bitcoin yet. There is only a minimal transfer system:\nwallets, balances, transaction rules, fees, and an accepted ledger history.\n\n**Double-spending pressure**\n\nThe first interesting problem is conflict. If Alice has 100 units and submits\ntwo transactions that each spend about 70 units, both transactions cannot settle\ninside the same ledger history:\n\n$$\nx_1+f_1+x_2+f_2 > \\mathrm{Balance}_A.\n$$\n\nThe system needs a rule for deciding which transaction enters the accepted\nhistory. In a centralized ledger the institution decides. In a blockchain\nnetwork, the accepted history comes from transaction validity, block inclusion,\nand consensus.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndouble_spend_attempt <- tibble(\n  tx_id = c(\"tx_A\", \"tx_B\"),\n  sender = c(\"Alice\", \"Alice\"),\n  receiver = c(\"Bob\", \"Carol\"),\n  amount = c(70, 70),\n  fee = c(0.20, 0.40)\n)\n\napply_ledger_order <- function(pool, balances, miner = \"Miner\") {\n  balances_after <- balances\n  outcome <- pool |>\n    mutate(total_spent = amount + fee, status = NA_character_)\n\n  for (k in seq_len(nrow(outcome))) {\n    tx <- outcome[k, ]\n    sender_balance <- balances_after$balance[\n      balances_after$wallet == tx$sender\n    ]\n\n    valid <- sender_balance >= tx$amount + tx$fee\n\n    if (valid) {\n      outcome$status[k] <- \"Accepted\"\n      balances_after$balance[balances_after$wallet == tx$sender] <-\n        balances_after$balance[balances_after$wallet == tx$sender] -\n        tx$amount - tx$fee\n      balances_after$balance[balances_after$wallet == tx$receiver] <-\n        balances_after$balance[balances_after$wallet == tx$receiver] +\n        tx$amount\n      balances_after$balance[balances_after$wallet == miner] <-\n        balances_after$balance[balances_after$wallet == miner] + tx$fee\n    } else {\n      outcome$status[k] <- \"Rejected\"\n    }\n  }\n\n  outcome\n}\n\ndouble_spend_outcome <- bind_rows(\n  apply_ledger_order(double_spend_attempt, balances) |>\n    mutate(ordering = \"tx_A arrives first\"),\n  apply_ledger_order(arrange(double_spend_attempt, desc(fee)), balances) |>\n    mutate(ordering = \"Higher fee arrives first\")\n)\n\ndouble_spend_outcome\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 8\n  tx_id sender receiver amount   fee total_spent status   ordering              \n  <chr> <chr>  <chr>     <dbl> <dbl>       <dbl> <chr>    <chr>                 \n1 tx_A  Alice  Bob          70   0.2        70.2 Accepted tx_A arrives first    \n2 tx_B  Alice  Carol        70   0.4        70.4 Rejected tx_A arrives first    \n3 tx_B  Alice  Carol        70   0.4        70.4 Accepted Higher fee arrives fi…\n4 tx_A  Alice  Bob          70   0.2        70.2 Rejected Higher fee arrives fi…\n```\n\n\n:::\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  double_spend_outcome,\n  aes(x = tx_id, y = total_spent, fill = status)\n) +\n  geom_col(width = 0.62) +\n  facet_wrap(~ ordering) +\n  scale_fill_manual(values = c(\"Accepted\" = \"#27AE60\",\n                               \"Rejected\" = \"#C0392B\")) +\n  labs(x = \"Transaction\", y = \"Amount plus fee\", fill = \"\")\n```\n\n::: {.cell-output-display}\n![](08-blockchain_files/figure-html/unnamed-chunk-3-1.png){width=691.2}\n:::\n:::\n\n\n\n\nThe figure shows why a shared ledger needs more than arithmetic. The balances\ntell us that the two transfers cannot both be final. The network still needs a\nmechanism for selecting one accepted history and rejecting conflicting updates.\n\n**Blocks**\n\nA block is a container for ledger information. In a simple teaching example,\nthe block contains:\n\n* a block number;\n* a timestamp;\n* transaction data;\n* the hash of the previous block;\n* a nonce used by proof-of-work examples;\n* the block's own hash.\n\nThe diagram below shows the structure.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nblock_boxes <- tibble(\n  label = c(\"Block number\", \"Timestamp\", \"Transactions\",\n            \"Previous hash\", \"Nonce\", \"Block hash\"),\n  xmin = c(0, 0, 0, 3.4, 3.4, 3.4),\n  xmax = c(3, 3, 3, 6.4, 6.4, 6.4),\n  ymin = c(2.4, 1.2, 0, 2.4, 1.2, 0),\n  ymax = c(3.2, 2, 0.8, 3.2, 2, 0.8)\n)\n\nggplot(block_boxes) +\n  geom_rect(\n    aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax),\n    fill = \"#EAF2F8\", color = \"#1B4F72\", linewidth = 0.7\n  ) +\n  geom_text(aes(x = (xmin + xmax) / 2, y = (ymin + ymax) / 2, label = label),\n            size = 3.7) +\n  annotate(\"text\", x = 3.2, y = 3.45, label = \"Block\", fontface = \"bold\",\n           size = 4.2) +\n  coord_cartesian(xlim = c(-0.1, 6.5), ylim = c(-0.05, 3.65), expand = FALSE) +\n  theme_void() +\n  theme(plot.margin = margin(0, 0, 0, 0))\n```\n\n::: {.cell-output-display}\n![A simplified block structure.](08-blockchain_files/figure-html/fig-block-structure-1.png){#fig-block-structure width=672}\n:::\n:::\n\n\n\n\nIn R, a block can be represented as a list.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nblock <- list(\n  number = 1,\n  timestamp = \"2026-01-01 09:00:00\",\n  data = \"Alice pays Bob 10\",\n  parent_hash = \"0\",\n  nonce = 0\n)\n\nblock\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$number\n[1] 1\n\n$timestamp\n[1] \"2026-01-01 09:00:00\"\n\n$data\n[1] \"Alice pays Bob 10\"\n\n$parent_hash\n[1] \"0\"\n\n$nonce\n[1] 0\n```\n\n\n:::\n:::\n\n\n\n\nThis object is not yet chained to any history. The next step is to compute a\nhash.\n\nThe block is useful because it gives the system a unit of agreement. Instead of\nasking the network to agree on each isolated transaction, the system asks the\nnetwork to agree on a batch of transactions and on the previous history that\nthe batch extends. This is why the parent hash is part of the block: it tells\nthe reader which past ledger the new block is trying to update.\n\n**Hashes**\n\nA cryptographic hash function maps an input of arbitrary size into a fixed-size\ndigest. A useful hash function for a ledger has three properties:\n\n* the same input gives the same hash;\n* a small change in the input gives a very different hash;\n* the input cannot be reconstructed from the hash in any practical way.\n\nFor block $i$, we can write the hash as:\n\n$$\nh_i = H(\\mathrm{data}_i, \\mathrm{parentHash}_i,\n\\mathrm{timestamp}_i, \\mathrm{nonce}_i).\n$$\n\nFor non-genesis blocks, the parent hash equals the hash of the previous block:\n\n$$\n\\mathrm{parentHash}_i = h_{i-1}.\n$$\n\nThe examples use SHA-256 through the <tt>`digest`</tt> package. SHA-256 returns\n256 bits, which correspond to 32 bytes or 64 hexadecimal characters.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndigest(\"Alice pays Bob 10\", algo = \"sha256\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"70e9b38977e6fb5acc5d40fba5ad2ad9b1f3bf14d8d5228785ecf10b17e506c5\"\n```\n\n\n:::\n\n```{.r .cell-code}\nnchar(digest(\"Alice pays Bob 10\", algo = \"sha256\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 64\n```\n\n\n:::\n:::\n\n\n\n\nA tiny change in the message changes the digest.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndigest(\"Alice pays Bob 10\", algo = \"sha256\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"70e9b38977e6fb5acc5d40fba5ad2ad9b1f3bf14d8d5228785ecf10b17e506c5\"\n```\n\n\n:::\n\n```{.r .cell-code}\ndigest(\"Alice pays Bob 11\", algo = \"sha256\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"d8e09925fe69f7daf20024e3f9150602c534171f47425301235f81b76555f93a\"\n```\n\n\n:::\n:::\n\n\n\n\nThe following helper creates a block and then hashes the full block content.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhash_block <- function(block) {\n  block$hash <- NULL\n  digest(block, algo = \"sha256\")\n}\n\ncreate_block <- function(number, data, parent_hash = \"0\",\n                         nonce = 0, timestamp = Sys.time()) {\n  block <- list(\n    number = number,\n    timestamp = as.character(timestamp),\n    data = data,\n    parent_hash = parent_hash,\n    nonce = nonce\n  )\n  block$hash <- hash_block(block)\n  block\n}\n\nblock1 <- create_block(\n  number = 1,\n  data = \"Genesis block\",\n  parent_hash = \"0\",\n  timestamp = \"2026-01-01 09:00:00\"\n)\n\nblock2 <- create_block(\n  number = 2,\n  data = \"Alice pays Bob 10\",\n  parent_hash = block1$hash,\n  timestamp = \"2026-01-01 09:01:00\"\n)\n\nblock3 <- create_block(\n  number = 3,\n  data = \"Bob pays Carol 5\",\n  parent_hash = block2$hash,\n  timestamp = \"2026-01-01 09:02:00\"\n)\n\nchain <- list(block1, block2, block3)\nchain\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[[1]]\n[[1]]$number\n[1] 1\n\n[[1]]$timestamp\n[1] \"2026-01-01 09:00:00\"\n\n[[1]]$data\n[1] \"Genesis block\"\n\n[[1]]$parent_hash\n[1] \"0\"\n\n[[1]]$nonce\n[1] 0\n\n[[1]]$hash\n[1] \"884045791d17ac206a102ea75982a9c180abc6c8194b4401662c42281c0949af\"\n\n\n[[2]]\n[[2]]$number\n[1] 2\n\n[[2]]$timestamp\n[1] \"2026-01-01 09:01:00\"\n\n[[2]]$data\n[1] \"Alice pays Bob 10\"\n\n[[2]]$parent_hash\n[1] \"884045791d17ac206a102ea75982a9c180abc6c8194b4401662c42281c0949af\"\n\n[[2]]$nonce\n[1] 0\n\n[[2]]$hash\n[1] \"d6ab66a54c32a8848e2855157dd63484714b00205d048afe8400308ff15c8b01\"\n\n\n[[3]]\n[[3]]$number\n[1] 3\n\n[[3]]$timestamp\n[1] \"2026-01-01 09:02:00\"\n\n[[3]]$data\n[1] \"Bob pays Carol 5\"\n\n[[3]]$parent_hash\n[1] \"d6ab66a54c32a8848e2855157dd63484714b00205d048afe8400308ff15c8b01\"\n\n[[3]]$nonce\n[1] 0\n\n[[3]]$hash\n[1] \"3db2457c9284aeb03a527f07087ccc8c16207b0cb41c78540d0c666fa9ef1393\"\n```\n\n\n:::\n:::\n\n\n\n\nThe financial meaning of this construction is integrity. A later block commits\nto the hash of the previous block. If an old transaction changes, the old hash\nchanges, and the later blocks no longer point to the same history.\n\nThis is a commitment mechanism. A block hash is a compact fingerprint of the\nblock content. A parent hash is a compact fingerprint of the previous accepted\nhistory. Linking those fingerprints creates a chain of commitments: the latest\nblock implicitly refers to every earlier accepted block through the sequence of\nhashes.\n\n**Chain validation**\n\nA basic validation rule checks two conditions:\n\n* each block hash matches the content of that block;\n* each non-genesis block points to the hash of the previous block.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvalidate_chain <- function(chain) {\n  for (i in seq_along(chain)) {\n    block <- chain[[i]]\n    declared_hash <- block$hash\n    expected_hash <- hash_block(block)\n\n    if (!identical(declared_hash, expected_hash)) {\n      return(FALSE)\n    }\n\n    if (i == 1 && block$parent_hash != \"0\") {\n      return(FALSE)\n    }\n\n    if (i > 1 && block$parent_hash != chain[[i - 1]]$hash) {\n      return(FALSE)\n    }\n  }\n\n  TRUE\n}\n\nvalidate_chain(chain)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n:::\n\n\n\n\nThe chain can be drawn as a sequence of blocks connected by hashes.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nchain_plot <- tibble(\n  block = c(\"Block 1\", \"Block 2\", \"Block 3\"),\n  x = c(1, 4, 7),\n  y = 1,\n  hash = c(\"h1\", \"h2\", \"h3\")\n)\n\nggplot(chain_plot) +\n  geom_rect(\n    aes(xmin = x - 0.9, xmax = x + 0.9, ymin = y - 0.45, ymax = y + 0.45),\n    fill = \"#E8F6F3\", color = \"#117864\", linewidth = 0.8\n  ) +\n  geom_text(aes(x = x, y = y + 0.12, label = block), fontface = \"bold\",\n            size = 3.8) +\n  geom_text(aes(x = x, y = y - 0.16, label = paste0(\"hash = \", hash)),\n            size = 3.2) +\n  geom_segment(\n    data = tibble(x = c(1.9, 4.9), xend = c(3.1, 6.1), y = 1, yend = 1),\n    aes(x = x, xend = xend, y = y, yend = yend),\n    arrow = grid::arrow(length = grid::unit(0.18, \"inches\")),\n    color = \"#566573\", linewidth = 0.8\n  ) +\n  annotate(\"text\", x = 2.5, y = 1.33, label = \"parent_hash = h1\", size = 3) +\n  annotate(\"text\", x = 5.5, y = 1.33, label = \"parent_hash = h2\", size = 3) +\n  coord_cartesian(xlim = c(0, 8), ylim = c(0.35, 1.6), expand = FALSE) +\n  theme_void() +\n  theme(plot.margin = margin(0, 0, 0, 0))\n```\n\n::: {.cell-output-display}\n![Blocks linked by parent hashes.](08-blockchain_files/figure-html/fig-chain-hashes-1.png){#fig-chain-hashes width=691.2}\n:::\n:::\n\n\n\n\nNow change an old block.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntampered_chain <- chain\ntampered_chain[[2]]$data <- \"Alice pays Bob 100\"\n\nvalidate_chain(tampered_chain)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n:::\n\n\n\n\nThe validation fails because the declared hash of block 2 no longer matches\nthe altered content. If block 2 is rehashed, block 3 still points to the old\nblock 2 hash.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntampered_rehashed <- tampered_chain\ntampered_rehashed[[2]]$hash <- hash_block(tampered_rehashed[[2]])\n\nvalidate_chain(tampered_rehashed)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n:::\n\n\n\n\nThis is the core of the chain idea. A historical alteration requires updating\nthe altered block and every later block. In a real network, the attacker would\nalso need the rest of the network to accept that altered history.\n\nThe toy validation function checks structural consistency. Real systems add\nmany more checks: transaction signatures, double-spending rules, block-size or\nblock-weight limits, timestamp rules, consensus-specific constraints, and\nnetwork-level acceptance. The simplified function is still useful because it\nshows the main idea: a valid ledger history is a sequence whose content and\nlinks agree.\n\n**Proof-of-work**\n\nHashes are fast to compute, so hashing alone does not make history expensive\nto rewrite. Proof-of-work adds a cost. A miner searches for a nonce such that\nthe block hash satisfies a difficulty condition. In the toy rule below, the\nhash must start with $d$ zeros:\n\n$$\n\\operatorname{substr}(H(\\mathrm{block}, \\mathrm{nonce}),1,d)\n=\\underbrace{00\\cdots0}_{d\\ \\mathrm{zeros}}.\n$$\n\nVerification is cheap because any node can hash the block once and check the\nleading zeros. Finding a valid nonce requires repeated trials.\n\nThis asymmetry is the key idea. Producing the proof is costly; checking it is\ncheap. In a financial ledger, that asymmetry can be used to make accepted\nhistory costly to replace. The cost does not come from secrecy. Everyone can\nsee the block and verify the proof. The cost comes from the amount of work\nneeded to find an alternative valid chain.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nproof_of_work <- function(block, difficulty) {\n  target <- paste(rep(\"0\", difficulty), collapse = \"\")\n  nonce <- 0\n\n  repeat {\n    block$nonce <- nonce\n    candidate_hash <- hash_block(block)\n\n    if (substr(candidate_hash, 1, difficulty) == target) {\n      return(list(nonce = nonce, hash = candidate_hash))\n    }\n\n    nonce <- nonce + 1\n  }\n}\n\npow_block <- list(\n  number = 4,\n  timestamp = \"2026-01-01 09:03:00\",\n  data = \"Carol pays Dave 3\",\n  parent_hash = block3$hash\n)\n\nproof_of_work(pow_block, difficulty = 2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$nonce\n[1] 76\n\n$hash\n[1] \"00925cdedbb657f66423bbd706e861e92579d5a5d32aaae012ec5723f642df86\"\n```\n\n\n:::\n:::\n\n\n\n\nThe number of trials tends to rise quickly with difficulty.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndifficulty_table <- tibble(difficulty = 1:4) |>\n  rowwise() |>\n  mutate(nonce = proof_of_work(pow_block, difficulty)$nonce) |>\n  ungroup()\n\ndifficulty_table\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 2\n  difficulty  nonce\n       <int>  <dbl>\n1          1      1\n2          2     76\n3          3   4583\n4          4 106604\n```\n\n\n:::\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(difficulty_table, aes(x = difficulty, y = nonce)) +\n  geom_line(color = \"#7B241C\", linewidth = 1) +\n  geom_point(color = \"#C0392B\", size = 3) +\n  scale_x_continuous(breaks = difficulty_table$difficulty) +\n  scale_y_continuous(labels = scales::comma) +\n  labs(x = \"Difficulty: required leading zeros\", y = \"Nonce found\")\n```\n\n::: {.cell-output-display}\n![Proof-of-work trials under increasing toy difficulty.](08-blockchain_files/figure-html/fig-pow-difficulty-1.png){#fig-pow-difficulty width=672}\n:::\n:::\n\n\n\n\nBitcoin is the canonical proof-of-work case. Miners gather valid transactions,\nconstruct candidate blocks, search for a valid nonce, and broadcast the block\nwhen the proof is found. Ethereum is useful as a contrast because it moved from\nproof-of-work to proof-of-stake in 2022. This chapter keeps proof-of-work\nbecause it makes the cost of rewriting history visible in a small R example.\n\n**Transactions and fees**\n\nTransactions are the financial content of blocks. A pending transaction has a\nsender, receiver, amount, and fee. The fee compensates the block producer for\nincluding the transaction.\n\nThe ledger should reject a transaction for three basic reasons in this toy\nmodel. First, the sender may not have enough balance. Second, the transaction\nmay have an invalid amount or fee. Third, the block may already be full. These\nare different failures. Insufficient balance is an accounting failure. A\nnegative amount or fee is a rule failure. A full block is a capacity failure.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\npending_transactions <- tibble(\n  tx_id = c(\"tx1\", \"tx2\", \"tx3\", \"tx4\", \"tx5\"),\n  sender = c(\"Alice\", \"Bob\", \"Carol\", \"Dave\", \"Alice\"),\n  receiver = c(\"Bob\", \"Carol\", \"Alice\", \"Bob\", \"Carol\"),\n  amount = c(25, 20, 12, 12, 70),\n  fee = c(0.40, 0.15, 0.80, 0.30, 0.60)\n)\n\npending_transactions\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 5\n  tx_id sender receiver amount   fee\n  <chr> <chr>  <chr>     <dbl> <dbl>\n1 tx1   Alice  Bob          25  0.4 \n2 tx2   Bob    Carol        20  0.15\n3 tx3   Carol  Alice        12  0.8 \n4 tx4   Dave   Bob          12  0.3 \n5 tx5   Alice  Carol        70  0.6 \n```\n\n\n:::\n:::\n\n\n\n\nIn many blockchain systems, transactions waiting for inclusion sit in a pending\npool. A block producer may prefer transactions with higher fees, subject to\nvalidity and block capacity. If the block can include $m$ transactions, a\nsimple selection rule is:\n\n$$\nT^* = \\operatorname{top}_m(\\mathrm{fee}_j)\n\\quad \\mathrm{among\\ valid\\ pending\\ transactions}.\n$$\n\nThe toy function below processes transactions in descending fee order and\nupdates balances sequentially.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nselect_for_block <- function(pool, balances, block_size = 3,\n                             miner = \"Miner\") {\n  ordered_pool <- arrange(pool, desc(fee))\n  accepted <- ordered_pool[0, ]\n  rejected <- ordered_pool[0, ]\n  balances_after <- balances\n\n  for (k in seq_len(nrow(ordered_pool))) {\n    tx <- ordered_pool[k, ]\n\n    if (nrow(accepted) >= block_size) {\n      rejected <- bind_rows(rejected, tx)\n      next\n    }\n\n    sender_balance <- balances_after$balance[\n      balances_after$wallet == tx$sender\n    ]\n\n    valid <- length(sender_balance) == 1 &&\n      sender_balance >= tx$amount + tx$fee &&\n      tx$amount > 0 &&\n      tx$fee >= 0\n\n    if (valid) {\n      accepted <- bind_rows(accepted, tx)\n      balances_after$balance[balances_after$wallet == tx$sender] <-\n        balances_after$balance[balances_after$wallet == tx$sender] -\n        tx$amount - tx$fee\n      balances_after$balance[balances_after$wallet == tx$receiver] <-\n        balances_after$balance[balances_after$wallet == tx$receiver] +\n        tx$amount\n      balances_after$balance[balances_after$wallet == miner] <-\n        balances_after$balance[balances_after$wallet == miner] + tx$fee\n    } else {\n      rejected <- bind_rows(rejected, tx)\n    }\n  }\n\n  list(accepted = accepted, rejected = rejected, balances = balances_after)\n}\n\nblock_selection <- select_for_block(\n  pending_transactions,\n  balances,\n  block_size = 3,\n  miner = \"Miner\"\n)\n\nblock_selection$accepted\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 3 × 5\n  tx_id sender receiver amount   fee\n  <chr> <chr>  <chr>     <dbl> <dbl>\n1 tx3   Carol  Alice        12   0.8\n2 tx5   Alice  Carol        70   0.6\n3 tx1   Alice  Bob          25   0.4\n```\n\n\n:::\n\n```{.r .cell-code}\nblock_selection$rejected\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 5\n  tx_id sender receiver amount   fee\n  <chr> <chr>  <chr>     <dbl> <dbl>\n1 tx4   Dave   Bob          12  0.3 \n2 tx2   Bob    Carol        20  0.15\n```\n\n\n:::\n\n```{.r .cell-code}\nblock_selection$balances\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 2\n  wallet balance\n  <chr>    <dbl>\n1 Alice     16  \n2 Bob       60  \n3 Carol     72.2\n4 Dave       5  \n5 Miner      1.8\n```\n\n\n:::\n:::\n\n\n\n\nThe accepted transactions are valid under the balance rule and fit inside the\nblock capacity. The rejected transactions either fail the validity test or are\nleft outside the block capacity in this simple example.\n\nThis small example also shows why fees matter. If several valid transactions\ncompete for limited block capacity, a fee ranking can influence which transfers\nenter first. In production systems the selection rule can be more complex, but\nthe economic intuition is already visible: users pay for inclusion, and block\nproducers respond to those incentives.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfee_competition <- pending_transactions |>\n  mutate(\n    status = if_else(tx_id %in% block_selection$accepted$tx_id,\n                     \"Included in block\", \"Left out or invalid\"),\n    tx_id = reorder(tx_id, fee)\n  )\n\nggplot(fee_competition, aes(x = tx_id, y = fee, fill = status)) +\n  geom_col(width = 0.65) +\n  coord_flip() +\n  scale_fill_manual(values = c(\"Included in block\" = \"#2E86C1\",\n                               \"Left out or invalid\" = \"#BFC9CA\")) +\n  labs(x = \"Pending transaction\", y = \"Fee\", fill = \"\")\n```\n\n::: {.cell-output-display}\n![](08-blockchain_files/figure-html/unnamed-chunk-15-1.png){width=672}\n:::\n:::\n\n\n\n\nThis turns block construction into a market for scarce inclusion. The fee is\nsmall relative to the transfer amount, but it can decide timing when many\ntransactions compete for limited capacity. This is one reason public blockchain\ndata can be financially rich: pending transactions, fees, and inclusion delays\nreveal pressure on the settlement layer.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbalance_plot <- bind_rows(\n  mutate(balances, state = \"Before block\"),\n  mutate(block_selection$balances, state = \"After block\")\n)\n\nggplot(balance_plot, aes(x = wallet, y = balance, fill = state)) +\n  geom_col(position = \"dodge\", width = 0.68) +\n  scale_fill_manual(values = c(\"Before block\" = \"#2C7FB8\",\n                               \"After block\" = \"#F39C12\")) +\n  labs(x = \"Wallet\", y = \"Balance\", fill = \"\")\n```\n\n::: {.cell-output-display}\n![Wallet balances before and after the mined block.](08-blockchain_files/figure-html/fig-wallet-balances-1.png){#fig-wallet-balances width=672}\n:::\n:::\n\n\n\n\nThe miner balance increases by the sum of the fees included in the block:\n\n$$\n\\mathrm{Reward}_{\\mathrm{fees}}\n=\\sum_{j\\in T^*} \\mathrm{fee}_j.\n$$\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsum(block_selection$accepted$fee)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.8\n```\n\n\n:::\n:::\n\n\n\n\nIn Bitcoin, miners can also receive a block subsidy according to the issuance\nschedule. In this toy account-based model, only transaction fees are shown.\n\n**Mining a transaction block**\n\nThe accepted transactions can now become block data. The block hash commits to\nthe transaction set, the parent hash, the timestamp, and the nonce.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncandidate_block <- list(\n  number = 4,\n  timestamp = \"2026-01-01 09:03:00\",\n  data = block_selection$accepted,\n  parent_hash = block3$hash\n)\n\npow_result <- proof_of_work(candidate_block, difficulty = 2)\ncandidate_block$nonce <- pow_result$nonce\ncandidate_block$hash <- pow_result$hash\n\ntransaction_chain <- c(chain, list(candidate_block))\n\nvalidate_chain(transaction_chain)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\ncandidate_block\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n$number\n[1] 4\n\n$timestamp\n[1] \"2026-01-01 09:03:00\"\n\n$data\n# A tibble: 3 × 5\n  tx_id sender receiver amount   fee\n  <chr> <chr>  <chr>     <dbl> <dbl>\n1 tx3   Carol  Alice        12   0.8\n2 tx5   Alice  Carol        70   0.6\n3 tx1   Alice  Bob          25   0.4\n\n$parent_hash\n[1] \"3db2457c9284aeb03a527f07087ccc8c16207b0cb41c78540d0c666fa9ef1393\"\n\n$nonce\n[1] 349\n\n$hash\n[1] \"0040ac8e1ad29d021ce82050ec50faf5513247ee4512b0e63b72d2e2085a298a\"\n```\n\n\n:::\n:::\n\n\n\n\nChanging a transaction after the block is mined breaks the chain.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\naltered_transaction_chain <- transaction_chain\naltered_transaction_chain[[4]]$data$amount[1] <- 250\n\nvalidate_chain(altered_transaction_chain)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n:::\n\n\n\n\nThis example connects three financial objects: pending transactions, balances,\nand the accepted ledger state. The hash does not make the transaction true by\nitself. The validation rules, signatures, balance checks, consensus rule, and\nnetwork acceptance determine which transactions enter the ledger.\n\nThe example also separates two views of the same system. The transaction view\nasks which transfers are accepted. The state view asks what the balances are\nafter those transfers. Financial applications need both views: the history is\nneeded for auditability, while the state is needed for valuation, reporting,\nand risk controls.\n\n**Digital signatures**\n\nA transaction also needs authorization. Digital signatures use public-key\ncryptography to connect a message with a private key. The sender signs the\ntransaction with a private key. Anyone with the corresponding public key can\nverify that the signature matches the message.\n\nThis provides two properties:\n\n* authenticity: the signature matches the public key;\n* integrity: changing the message invalidates the signature.\n\nConfidentiality is a separate issue. A signature verifies who authorized a\nmessage and whether it changed. Encryption hides message contents from parties\nthat do not have the decryption key.\n\nIn ledger terms, a signature answers a narrow question: did the holder of the\nrelevant private key authorize this exact message? It does not prove that the\nsender is a real-world person, that the transfer is legal, or that the asset\nbehind a token exists. Those questions require identity, legal, operational,\nand governance layers outside the cryptographic signature.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nalice_key <- rsa_keygen()\nalice_public_key <- alice_key$pubkey\n\nsigned_transaction <- list(\n  sender = \"Alice\",\n  receiver = \"Bob\",\n  amount = 10,\n  fee = 0.1\n)\n\nserialized_transaction <- serialize(signed_transaction, NULL)\nsignature <- signature_create(serialized_transaction, sha256, key = alice_key)\n\nsignature_verify(\n  serialized_transaction,\n  signature,\n  sha256,\n  pubkey = alice_public_key\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n:::\n\n\n\n\nNow alter the transaction and verify the old signature against the altered\nmessage.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\naltered_signed_transaction <- list(\n  sender = \"Alice\",\n  receiver = \"Carol\",\n  amount = 10,\n  fee = 0.1\n)\n\naltered_serialized_transaction <- serialize(altered_signed_transaction, NULL)\n\ntryCatch(\n  signature_verify(\n    altered_serialized_transaction,\n    signature,\n    sha256,\n    pubkey = alice_public_key\n  ),\n  error = function(e) FALSE\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n:::\n\n\n\n\nThe altered message fails verification. This is why signatures are part of the\nfinancial logic of a blockchain: the ledger should only accept transactions\nauthorized by the holder of the relevant private key.\n\n**Network and consensus**\n\nA blockchain ledger is distributed over a peer-to-peer network. A simplified\nproof-of-work workflow is:\n\n* users broadcast transactions;\n* nodes check transaction validity;\n* miners gather valid pending transactions into candidate blocks;\n* miners search for a proof-of-work;\n* the first valid block is broadcast;\n* nodes accept the block if the proof and the transactions are valid;\n* new blocks build on the accepted parent hash.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnetwork_nodes <- tibble(\n  id = c(\"wallet_a\", \"wallet_b\", \"wallet_c\", \"pool\",\n         \"miner_1\", \"miner_2\", \"node_1\", \"node_2\", \"node_3\", \"ledger\"),\n  label = c(\"Wallet A\", \"Wallet B\", \"Wallet C\", \"Pending\\npool\",\n            \"Miner 1\", \"Miner 2\", \"Full node\\n1\", \"Full node\\n2\",\n            \"Full node\\n3\", \"Accepted\\nledger\"),\n  type = c(\"Wallet\", \"Wallet\", \"Wallet\", \"Pool\",\n           \"Miner\", \"Miner\", \"Node\", \"Node\", \"Node\", \"Ledger\"),\n  x = c(0, 0, 0, 2.2, 4.3, 4.3, 6.5, 6.5, 6.5, 8.7),\n  y = c(3, 2, 1, 2, 2.7, 1.3, 3, 2, 1, 2)\n)\n\nnetwork_edges <- tibble(\n  from = c(\"wallet_a\", \"wallet_b\", \"wallet_c\", \"pool\", \"pool\",\n           \"miner_1\", \"miner_2\", \"miner_1\", \"miner_2\",\n           \"node_1\", \"node_2\", \"node_3\"),\n  to = c(\"pool\", \"pool\", \"pool\", \"miner_1\", \"miner_2\",\n         \"node_1\", \"node_3\", \"node_2\", \"node_2\",\n         \"ledger\", \"ledger\", \"ledger\")\n) |>\n  left_join(network_nodes |> select(from = id, x, y), by = \"from\") |>\n  rename(x_from = x, y_from = y) |>\n  left_join(network_nodes |> select(to = id, x, y), by = \"to\") |>\n  rename(x_to = x, y_to = y)\n\nggplot() +\n  geom_segment(\n    data = network_edges,\n    aes(x = x_from, y = y_from, xend = x_to, yend = y_to),\n    arrow = grid::arrow(length = grid::unit(0.12, \"inches\")),\n    color = \"#7F8C8D\", linewidth = 0.65, alpha = 0.9\n  ) +\n  geom_point(\n    data = network_nodes,\n    aes(x = x, y = y, fill = type),\n    shape = 21, size = 12, color = \"white\", stroke = 1.1\n  ) +\n  geom_text(\n    data = network_nodes,\n    aes(x = x, y = y, label = label),\n    size = 2.95, lineheight = 0.88\n  ) +\n  scale_fill_manual(values = c(\n    \"Wallet\" = \"#D6EAF8\",\n    \"Pool\" = \"#FCF3CF\",\n    \"Miner\" = \"#FADBD8\",\n    \"Node\" = \"#D5F5E3\",\n    \"Ledger\" = \"#E8DAEF\"\n  )) +\n  coord_cartesian(xlim = c(-0.45, 9.1), ylim = c(0.65, 3.35), expand = FALSE) +\n  guides(fill = \"none\") +\n  theme_void() +\n  theme(plot.margin = margin(0, 0, 0, 0))\n```\n\n::: {.cell-output-display}\n![A simplified peer-to-peer blockchain network.](08-blockchain_files/figure-html/fig-network-flow-1.png){#fig-network-flow width=825.6}\n:::\n:::\n\n\n\n\nThe diagram is a simplified network view. Wallets submit transactions to the\npending pool. Miners collect transactions and propose blocks. Full nodes verify\nthe candidate block and the ledger rules. The accepted ledger represents the\nhistory that nodes are willing to extend.\n\nConsensus is the rule that tells nodes which valid history to extend. In\nBitcoin, the accepted history is tied to accumulated proof-of-work. The economic\nsecurity argument is that rewriting history requires controlling enough\ncomputational work to replace the accepted chain, which is costly.\n\nThis is also where incentives matter. Fees and block rewards encourage miners\nto spend resources on valid blocks. Invalid blocks waste effort because nodes\nreject them.\n\n## Bitcoin case: a proof-of-work ledger\n\nAt this point the chapter has built a generic blockchain mechanism. Bitcoin is\nthe next step because it turns that mechanism into a specific monetary and\npayment system. Its design connects payments, proof-of-work, scarce issuance,\npublic verification, and private-key custody [@nakamoto2008bitcoin].\n\nThe generic model and Bitcoin share the same broad workflow:\n\n$$\n\\mathrm{signed\\ transaction}\n\\rightarrow\n\\mathrm{pending\\ transaction\\ set}\n\\rightarrow\n\\mathrm{candidate\\ block}\n\\rightarrow\n\\mathrm{proof\\ of\\ work}\n\\rightarrow\n\\mathrm{network\\ validation}\n\\rightarrow\n\\mathrm{accepted\\ ledger\\ history}.\n$$\n\nThe differences are important:\n\n| Generic model in this chapter | Bitcoin case |\n|---|---|\n| Account balances are stored directly for teaching clarity | Balances are inferred from unspent transaction outputs |\n| Transactions move amounts from sender to receiver | Transactions consume previous outputs and create new outputs |\n| The miner receives transaction fees in the toy balance table | Miners can receive fees and a protocol-defined block subsidy |\n| Difficulty is a small number of leading zeros | Difficulty adjusts so blocks arrive near the protocol target interval |\n| Finality is shown as a valid chain in R | Settlement confidence grows as more blocks build on top |\n\nThe relevant Bitcoin network roles can be represented as nodes rather than as a\nsingle line of steps. A wallet creates a signed transaction and broadcasts it.\nThe mempool is the waiting area for valid pending transactions. Miners convert\nsome of those transactions into candidate blocks. Full nodes check the proposed\nblock against the consensus rules. Exchanges, custodians, merchants, and block\nexplorers sit outside the core protocol while remaining financially important\nas common points of access, monitoring, and settlement interpretation.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbitcoin_nodes <- tibble(\n  id = c(\"payer\", \"receiver\", \"mempool\", \"miner\", \"full_nodes\",\n         \"chain\", \"exchange\", \"explorer\"),\n  label = c(\"Payer\\nwallet\", \"Receiver\\nwallet\", \"Mempool\", \"Miner\",\n            \"Full\\nnodes\", \"Accepted\\nchain\", \"Exchange/\\ncustodian\",\n            \"Explorer/\\nanalytics\"),\n  type = c(\"Wallet\", \"Wallet\", \"Pool\", \"Miner\", \"Node\",\n           \"Ledger\", \"Access\", \"Analytics\"),\n  x = c(0.4, 7.7, 2.3, 4.1, 5.9, 7.7, 0.4, 5.9),\n  y = c(2.5, 0.8, 1.65, 2.45, 1.65, 1.65, 0.8, 0.45)\n)\n\nbitcoin_edges <- tibble(\n  from = c(\"payer\", \"mempool\", \"miner\", \"full_nodes\", \"chain\",\n           \"exchange\", \"chain\", \"full_nodes\"),\n  to = c(\"mempool\", \"miner\", \"full_nodes\", \"chain\", \"receiver\",\n         \"mempool\", \"explorer\", \"exchange\")\n) |>\n  left_join(bitcoin_nodes |> select(from = id, x, y), by = \"from\") |>\n  rename(x_from = x, y_from = y) |>\n  left_join(bitcoin_nodes |> select(to = id, x, y), by = \"to\") |>\n  rename(x_to = x, y_to = y)\n\nggplot() +\n  geom_segment(\n    data = bitcoin_edges,\n    aes(x = x_from, y = y_from, xend = x_to, yend = y_to),\n    arrow = grid::arrow(length = grid::unit(0.11, \"inches\")),\n    color = \"#7F8C8D\", linewidth = 0.6, alpha = 0.9\n  ) +\n  geom_point(\n    data = bitcoin_nodes,\n    aes(x = x, y = y, fill = type),\n    shape = 21, size = 12, color = \"white\", stroke = 1.1\n  ) +\n  geom_text(\n    data = bitcoin_nodes,\n    aes(x = x, y = y, label = label),\n    size = 2.85, lineheight = 0.88\n  ) +\n  scale_fill_manual(values = c(\n    \"Wallet\" = \"#D6EAF8\",\n    \"Pool\" = \"#FCF3CF\",\n    \"Miner\" = \"#FADBD8\",\n    \"Node\" = \"#D5F5E3\",\n    \"Ledger\" = \"#E8DAEF\",\n    \"Access\" = \"#FDEBD0\",\n    \"Analytics\" = \"#EAECEE\"\n  )) +\n  coord_cartesian(xlim = c(-0.2, 8.2), ylim = c(0.05, 2.95), expand = FALSE) +\n  guides(fill = \"none\") +\n  theme_void() +\n  theme(plot.margin = margin(0, 0, 0, 0))\n```\n\n::: {.cell-output-display}\n![Relevant roles in a simplified Bitcoin network.](08-blockchain_files/figure-html/fig-bitcoin-network-roles-1.png){#fig-bitcoin-network-roles width=806.4}\n:::\n:::\n\n\n\n\nThe diagram separates protocol roles from financial access roles. Wallets,\nminers, full nodes, and the accepted chain describe the transaction validation\npath. Exchanges and custodians describe how many users actually enter the\nsystem. Explorers and analytics tools describe how public data becomes\nobservable, searchable, and useful for monitoring.\n\nThe account-based model says that Alice has a balance and can send part of it.\nBitcoin uses a UTXO model. A UTXO is an unspent transaction output. If Alice\ncontrols previous outputs worth 0.8 BTC and 0.4 BTC, and wants to pay 0.9 BTC,\nher transaction spends enough previous outputs and creates new outputs: one for\nthe receiver, one for change back to Alice, and one implicit fee to the miner.\nThe balance is therefore reconstructed from the set of unspent outputs\ncontrolled by Alice's keys.\n\nThe simplified accounting equation is:\n\n$$\n\\sum \\mathrm{Inputs} = \\sum \\mathrm{Outputs} + \\mathrm{Fee}.\n$$\n\nThe fee is the difference between the value consumed by the transaction and the\nvalue assigned to new outputs:\n\n$$\n\\mathrm{Fee} = \\sum \\mathrm{Inputs} - \\sum \\mathrm{Outputs}.\n$$\n\nThis differs from the account example above, where the fee was an explicit\ncolumn. The economic meaning is similar: the sender gives up value, the\nreceiver obtains value, and the miner has an incentive to include the\ntransaction.\n\nBitcoin also makes proof-of-work part of monetary security. A miner proposes a\nblock and searches for a valid proof. Nodes verify the block by checking the\nproof, transaction validity, and the rule that spent outputs have not already\nbeen spent. A transaction with one confirmation is included in one block. More\nconfirmations mean that additional blocks have been built on top of that block,\nraising the cost of replacing that history.\n\nThe settlement idea is probabilistic. A payment becomes harder to reverse as\nmore proof-of-work accumulates above it. This is why confirmations matter in\nBitcoin payments: they are a practical measure of how deeply a transaction sits\ninside the accepted history.\n\nOne simplified way to visualize the intuition is a catch-up race. Suppose an\nattacker controls share $q$ of mining power, with $0<q<0.5$, and an honest\nchain is already $z$ confirmations ahead. A compact approximation for the\nprobability that the attacker catches up is:\n\n$$\n\\Pr(\\mathrm{catchUp}\\mid z,q)\n\\approx\n\\left(\\frac{q}{1-q}\\right)^z.\n$$\n\nThe approximation is a teaching device. It captures the central shape: when the\nattacker has less than half of total work, each additional confirmation lowers\nthe catch-up probability.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nconfirmation_grid <- as_tibble(expand.grid(\n  confirmations = 1:10,\n  attacker_share = c(0.10, 0.20, 0.30, 0.40)\n)) |>\n  mutate(\n    catch_up_probability =\n      (attacker_share / (1 - attacker_share))^confirmations,\n    attacker_label = paste0(\"q = \",\n                            scales::percent(attacker_share, accuracy = 1))\n  )\n\nggplot(\n  confirmation_grid,\n  aes(x = confirmations, y = catch_up_probability, color = attacker_label)\n) +\n  geom_line(linewidth = 0.9) +\n  geom_point(size = 2) +\n  scale_x_continuous(breaks = 1:10) +\n  scale_y_log10(labels = scales::label_scientific(digits = 2)) +\n  labs(x = \"Confirmations\",\n       y = \"Catch-up probability (log scale)\", color = \"\")\n```\n\n::: {.cell-output-display}\n![](08-blockchain_files/figure-html/unnamed-chunk-21-1.png){width=691.2}\n:::\n:::\n\n\n\n\nThe logarithmic scale makes the settlement intuition visible. Confirmations\ntranslate settlement depth into a practical risk measure: they express the cost\nof replacing recent history. The same payment can be operationally acceptable at different\nconfirmation depths depending on amount, counterparty risk, liquidity needs,\nand the recipient's tolerance for reversal risk.\n\nBitcoin is financially interesting for four reasons.\n\nFirst, it turns ledger maintenance into an incentive problem. Miners spend\nresources because accepted blocks can pay rewards and fees. Second, it makes\ncustody explicit: control of private keys is control over spending authority.\nThird, it creates a public audit trail, so anyone can verify the transaction\nhistory according to protocol rules. Fourth, it introduces a digital asset with\na rule-based issuance schedule, which makes scarcity part of the protocol\ndesign.\n\nThe R model above should be read as a simplified map. It captures blocks,\nhashes, proof-of-work, fees, validation, and ledger updates. It leaves out many\nBitcoin details: UTXO selection algorithms, script rules, block propagation,\ndifficulty adjustment mechanics, mempool policy, fee estimation, wallet\nsoftware, and network topology. The simplification is deliberate. The goal is\nto make the financial logic visible before adding production-level complexity.\n\n## Tokenization\n\nTokenization extends the ledger idea from native coins to other financial or\neconomic claims. A token can represent a unit in a digital asset, a claim on a\ncash-flow arrangement, a fund share, a loyalty point, a stablecoin balance, or\na record linked to an off-chain asset. The crucial point is that the token\nrecord and the legal or operational claim have to be aligned.\n\nThe shift from Bitcoin to tokenization changes the financial question. In\nBitcoin, the native asset and the ledger are part of the same protocol. In\ntokenization, the ledger records ownership units whose value often depends on\nsomething outside the ledger: cash, securities, real assets, fund shares,\ninvoices, commodities, or contractual rights. That creates two layers:\n\n| Layer | Question |\n|---|---|\n| On-chain record | Who holds how many tokens according to the ledger? |\n| Off-chain claim | What legal, economic, or operational right does each token represent? |\n\nThe on-chain record can be precise while the off-chain claim still requires\nlegal documentation, custody, audits, servicing, redemption rules, and dispute\nresolution. This is why tokenization is a finance topic as much as a technology\ntopic.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntoken_layers <- tibble(\n  id = c(\"asset\", \"legal\", \"issuer\", \"registry\", \"investors\", \"cashflow\"),\n  label = c(\"Underlying\\nasset\", \"Legal\\nclaim\", \"Issuer/\\nservicer\",\n            \"Token\\nregistry\", \"Investors\", \"Cash-flow\\nrule\"),\n  layer = c(\"Off-chain\", \"Off-chain\", \"Bridge\",\n            \"On-chain\", \"Market\", \"Finance\"),\n  x = c(0.5, 1.9, 3.3, 4.7, 6.1, 7.5),\n  y = c(1.5, 1.5, 1.5, 1.5, 1.5, 1.5)\n)\n\ntoken_edges <- tibble(\n  from = c(\"asset\", \"legal\", \"issuer\", \"registry\", \"investors\"),\n  to = c(\"legal\", \"issuer\", \"registry\", \"investors\", \"cashflow\")\n) |>\n  left_join(token_layers |> select(from = id, x, y), by = \"from\") |>\n  rename(x_from = x, y_from = y) |>\n  left_join(token_layers |> select(to = id, x, y), by = \"to\") |>\n  rename(x_to = x, y_to = y) |>\n  mutate(\n    x_from_plot = x_from + 0.36,\n    x_to_plot = x_to - 0.36\n  )\n\nggplot() +\n  geom_segment(\n    data = token_edges,\n    aes(x = x_from_plot, y = y_from, xend = x_to_plot, yend = y_to),\n    arrow = grid::arrow(length = grid::unit(0.11, \"inches\")),\n    color = \"#7F8C8D\", linewidth = 0.65\n  ) +\n  geom_point(\n    data = token_layers,\n    aes(x = x, y = y, fill = layer),\n    shape = 21, size = 14, color = \"white\", stroke = 1.1\n  ) +\n  geom_text(\n    data = token_layers,\n    aes(x = x, y = y, label = label),\n    size = 2.8, lineheight = 0.88\n  ) +\n  scale_fill_manual(values = c(\n    \"Off-chain\" = \"#D6EAF8\",\n    \"Bridge\" = \"#FDEBD0\",\n    \"On-chain\" = \"#D5F5E3\",\n    \"Market\" = \"#E8DAEF\",\n    \"Finance\" = \"#FCF3CF\"\n  )) +\n  coord_cartesian(xlim = c(-0.1, 8.1), ylim = c(1.35, 1.65),\n                  expand = FALSE, clip = \"off\") +\n  guides(fill = \"none\") +\n  theme_void() +\n  theme(plot.margin = margin(8, 2, 8, 2))\n```\n\n::: {.cell-output-display}\n![](08-blockchain_files/figure-html/unnamed-chunk-22-1.png){width=768}\n:::\n:::\n\n\n\n\nThis stack is where tokenization becomes a financial structure. The token\nregistry can say who holds units. Valuation and risk also depend on the asset,\nlegal claim, servicing process, transfer restrictions, and cash-flow rule. A\ntoken model is incomplete unless the financial claim represented by the token\nis explicit.\n\nIn a simple model, a token contract has:\n\n* an issuer;\n* a token name;\n* total supply;\n* holder balances;\n* transfer rules;\n* optional cash-flow or redemption rules.\n\nSuppose a hypothetical issuer creates 1,000 tokens linked to participation in a\nsmall financial asset pool.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntoken_metadata <- tibble(\n  token = \"POOL_A_TOKEN\",\n  underlying = \"Hypothetical asset-pool participation\",\n  total_supply = 1000,\n  unit_claim = \"1 token = 0.1% of the pool cash-flow rule\"\n)\n\ntoken_register <- tibble(\n  holder = c(\"Issuer\", \"Investor_A\", \"Investor_B\", \"Investor_C\"),\n  token_balance = c(1000, 0, 0, 0)\n)\n\ntoken_metadata\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 4\n  token        underlying                            total_supply unit_claim    \n  <chr>        <chr>                                        <dbl> <chr>         \n1 POOL_A_TOKEN Hypothetical asset-pool participation         1000 1 token = 0.1…\n```\n\n\n:::\n\n```{.r .cell-code}\ntoken_register\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 2\n  holder     token_balance\n  <chr>              <dbl>\n1 Issuer              1000\n2 Investor_A             0\n3 Investor_B             0\n4 Investor_C             0\n```\n\n\n:::\n:::\n\n\n\n\nToken transfers are ledger updates. If holder $a$ transfers $q$ tokens to\nholder $b$, the token register changes as:\n\n$$\n\\mathrm{Token}_a^{\\prime} = \\mathrm{Token}_a-q,\n\\qquad\n\\mathrm{Token}_b^{\\prime} = \\mathrm{Token}_b+q,\n\\qquad\n0 \\leq q \\leq \\mathrm{Token}_a.\n$$\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntransfer_tokens <- function(register, from, to, amount) {\n  from_balance <- register$token_balance[register$holder == from]\n\n  valid <- length(from_balance) == 1 &&\n    from_balance >= amount &&\n    amount > 0 &&\n    to %in% register$holder\n\n  if (!valid) {\n    stop(\"Invalid token transfer\")\n  }\n\n  register$token_balance[register$holder == from] <-\n    register$token_balance[register$holder == from] - amount\n  register$token_balance[register$holder == to] <-\n    register$token_balance[register$holder == to] + amount\n\n  register\n}\n\ntoken_register_after <- token_register |>\n  transfer_tokens(\"Issuer\", \"Investor_A\", 400) |>\n  transfer_tokens(\"Issuer\", \"Investor_B\", 350) |>\n  transfer_tokens(\"Issuer\", \"Investor_C\", 150) |>\n  transfer_tokens(\"Investor_A\", \"Investor_C\", 50)\n\ntoken_register_after\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 2\n  holder     token_balance\n  <chr>              <dbl>\n1 Issuer               100\n2 Investor_A           350\n3 Investor_B           350\n4 Investor_C           200\n```\n\n\n:::\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\ntoken_plot <- bind_rows(\n  mutate(token_register, state = \"Initial register\"),\n  mutate(token_register_after, state = \"After transfers\")\n)\n\nggplot(token_plot, aes(x = holder, y = token_balance, fill = state)) +\n  geom_col(position = \"dodge\", width = 0.68) +\n  scale_fill_manual(values = c(\"Initial register\" = \"#566573\",\n                               \"After transfers\" = \"#27AE60\")) +\n  labs(x = \"Holder\", y = \"Token balance\", fill = \"\")\n```\n\n::: {.cell-output-display}\n![Token ownership before and after transfers.](08-blockchain_files/figure-html/fig-token-register-1.png){#fig-token-register width=672}\n:::\n:::\n\n\n\n\nIf the pool distributes cash flow $C$, a proportional token rule allocates:\n\n$$\n\\mathrm{CashFlow}_h = \\frac{\\mathrm{Token}_h}{S}C,\n$$\n\nwhere $S$ is total token supply.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncash_flow <- 5000\n\ncash_distribution <- token_register_after |>\n  mutate(\n    ownership = token_balance / sum(token_balance),\n    cash_flow = ownership * cash_flow\n  )\n\ncash_distribution\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 4\n  holder     token_balance ownership cash_flow\n  <chr>              <dbl>     <dbl>     <dbl>\n1 Issuer               100      0.1        500\n2 Investor_A           350      0.35      1750\n3 Investor_B           350      0.35      1750\n4 Investor_C           200      0.2       1000\n```\n\n\n:::\n:::\n\n\n\n\nThis simple example shows why tokenization belongs in a financial modeling\ncourse. It combines a ledger, ownership units, transfer rules, and cash-flow\nallocation. The hard parts appear quickly: custody, investor rights, transfer\nrestrictions, identity checks, tax treatment, legal enforceability, valuation,\nand operational controls.\n\n## Privacy\n\nPublic blockchains make verification easier by exposing transaction history,\nbut that transparency creates privacy questions. A public address can be\npseudonymous, yet transaction patterns may still reveal relationships. If an\naddress is linked to a person or institution, past and future activity can\nbecome easier to trace.\n\nThe banking model usually limits transaction information to the parties, the\nbank, and regulators. A public blockchain makes more information visible to the\nnetwork. Privacy-preserving tools, address management, zero-knowledge proofs,\nand permissioned ledgers are attempts to balance auditability and privacy.\n\n## Advantages and limits\n\nThe chapter's main lesson is that blockchain is a ledger design. Its financial\ninterest comes from combining records, verification, incentives, and settlement\nrules in one protocol.\n\nPotential advantages include:\n\n* shared audit trail;\n* public verification of ledger history;\n* resistance to undetected alteration;\n* programmable transfers and token rules;\n* faster experimentation with digital assets and settlement logic.\n\nImportant limits include:\n\n* scalability and transaction cost;\n* privacy leakage;\n* governance disputes;\n* custody risk from lost or stolen private keys;\n* smart-contract or implementation errors;\n* regulatory uncertainty;\n* weak links between on-chain tokens and off-chain assets.\n\nThe strongest way to read blockchain in this book is as an extension of\nfinancial modeling. Prices, returns, portfolios, and VaR model market behavior.\nBlockchain models the record-keeping and transfer layer of financial value. The\ntechnical details matter because a small change in validation, incentives,\nprivacy, or token rights can change the financial meaning of the system.\n\n## Further visual resources\n\nThe following resources can be useful as visual complements to the R examples:\n\n* [Blockchain Explanation](https://youtu.be/J-ab9was1p0)\n* [How does a blockchain work - Simply Explained](https://youtu.be/SSo_EIwHSd4)\n* [Asymmetric Encryption - Simply explained](https://youtu.be/AQDCe585Lnc)\n* [Proof-of-Stake (vs proof-of-work)](https://youtu.be/M3EFi_POhps)\n* [Zero Knowledge Proof - ZKP](https://youtu.be/OcmvMs4AMbM)\n",
    "supporting": [
      "08-blockchain_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}